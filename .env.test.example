# Test environment (used automatically by `npm test`)
# Integration tests derive the test database from DB_NAME ("<DB_NAME>_test")
# or use TEST_DB_NAME if set. PostgreSQL must be reachable with the same
# DB_HOST/DB_PORT/DB_USER/DB_PASSWORD credentials as the main .env.
#
# TEST_DB_NAME=sneplus_live_test
